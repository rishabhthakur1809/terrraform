import {Component} from 'react';
import Api from './Api';

export default class Auth extends Component {
   
    // TODO: Extract the refreshToken process to an Authentication layer so that it's re-usable by all components and can contain user login process at a later date.
    
}